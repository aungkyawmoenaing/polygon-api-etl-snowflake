# polygon-api-etl-snowflake
This project connects to the Polygon API, extracts stock ticker data, and loads it into Snowflake. It handles pagination, stores historical data with timestamps, and can be scheduled to run automatically.
